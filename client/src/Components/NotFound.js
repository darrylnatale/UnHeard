import { Context } from "../Context";
import { useContext } from "react";
import Results from "./Results";

const NotFound = () => {

    

    return ( <>
            <p>
          Looks like that artist isn't on spotify! Here's what we could find online: 
          </p>
          
          </> );
}
 
export default NotFound;